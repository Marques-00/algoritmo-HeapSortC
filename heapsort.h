#ifndef HEAP_SORT_H
#define HEAP_SORT_H

// Ordena o vetor em ordem crescente usando Heap Sort.
void heapSort(int v[], int n);

// Imprime o vetor na tela 
void printArray(const int v[], int n);

#endif